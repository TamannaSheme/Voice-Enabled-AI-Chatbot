{
  "name": "jest-testrail-integration",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --reporters=default --reporters=./customJestReporter.js"
  },
  "devDependencies": {
    "jest": "^29.x",
    "jsdom": "^22.x"
  }
}